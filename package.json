{
  "name": "memos-card-view",
  "version": "0.1.0",
  "description": "Obsidian plugin for viewing and editing Memos records as cards.",
  "main": "dist/main.js",
  "scripts": {
    "dev": "node scripts/copy-assets.mjs && node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node scripts/copy-assets.mjs && node esbuild.config.mjs production",
    "package": "npm run build && node scripts/package-release.mjs",
    "version": "node scripts/sync-version.mjs && npm run build && git add package.json package-lock.json manifest.json versions.json browser-extension/manifest.json",
    "release": "npm version --tag-version-prefix=\"\"",
    "release:patch": "npm version patch --tag-version-prefix=\"\"",
    "release:minor": "npm version minor --tag-version-prefix=\"\"",
    "release:major": "npm version major --tag-version-prefix=\"\""
  },
  "keywords": [
    "obsidian",
    "memos"
  ],
  "author": "shendongchun",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.17.10",
    "esbuild": "^0.28.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.6.3"
  }
}
